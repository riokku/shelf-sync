export const environment = {
  production: true,
  supabaseUrl: 'https://ailqjqjrzhzspofoslpa.supabase.co',
  supabaseAnonKey: 'sb_publishable_TQGjim7HxESW-PQwX4slYw_m-ONzeEa',
  // See environment.ts's own comment — same placeholder, same real key to
  // swap in once one exists (this app's dev/prod builds already share one
  // Supabase project, so they share one Turnstile widget too).
  turnstileSiteKey: '1x00000000000000000000AA',
};
